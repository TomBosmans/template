# Template

## commands

Run `npm run command help` to see a list of available commands.
To get more info about specific command run `npm run command help <command>`.


## TODO
- Make swagger work with auth
- hmr doesn't work with storybook yet
- Make svg's react to dark/light mode

- frontend error with toast: flushSync was called from inside a lifecycle method. 
  known issue should be fixed later: https://github.com/mui/base-ui/issues/3442
