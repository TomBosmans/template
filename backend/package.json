{
  "name": "backend",
  "description": "",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch --no-warnings --import=tsx --env-file=../.env src/main.ts",
    "build": "tsc --noEmit && tsup src/main.ts --format esm --out-dir dist",
    "lint": "biome check .",
    "test": "node --test --no-warnings --import=tsx src/**/*.spec.ts",
    "preview": "node --no-warnings dist/main.js"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.9.0",
    "db": "file:../database",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "awilix": "^12.0.4",
    "mails": "file:../mails",
    "sift": "^17.1.3",
    "zod": "^3.24.1"
  }
}
